exports.installController = function(mainApp,common){

	mainApp.controller('home.introductController',['$scope','muiSupport','http','$rootScope','$route','cache',function($scope,muiSupport,http,$rootScope,$route,cache){
		/*私有属性区域*/
		

               
		/*控制器执行入口*/
		$scope.$on('$routeChangeSuccess',function(){
			
		});
	}]);

}